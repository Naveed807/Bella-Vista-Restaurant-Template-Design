
//Prompt
*Create a 'Menu' Page including *

* **Menu Page**
* that seamlessly integrates with our existing luxury restaurant theme. The design must strictly follow these specifications:"*
**Design Foundation**
1. **Color Scheme**
   * Primary: Use your defined variables (`--primary-color: #d4af37`, `--secondary-color: #2c3e50`)
   * Accents: Gold gradient (`--gradient-gold`), red accent (`--accent-color: #e74c3c`)
   * Background: Dark theme as default (`--bg-dark: #1a1a1a` with `--header-bg` transparency)
2. **Typography & Spacing**
   * Font: Inherit `'Segoe UI', Tahoma` from body
   * Text colors: `--text-light`/`--text-dark` based on active theme
   * Consistent `--transition: all 0.3s cubic-bezier()` for hover effects
3. **Visual Treatments**
   * Shadows: Use `--shadow-dark`/`--shadow-light` contextually
   * Borders: Subtle `--border-color` with theme-aware opacity
   * Icons/Buttons: Apply `--gradient-gold` for interactive elements
**Bootstrap 5 Implementation**
* **Layout**:
   * Mobile-first grid (`container > row > col-*`) with your existing breakpoints
   * Spacing utilities (`py-4`, `px-md-5`) matching header/About sections
* **Components**:
   * Cards with gold hover effects (scale + translateY)
   * Badges using `--accent-color` for "Chef's Pick" tags
   * Buttons with `--gradient-gold` and dark text
* **Responsiveness**:
   * Mirror header's `d-none`/`d-lg-flex` logic for dish descriptions
   * Ensure images scale proportionally (use `img-fluid`)
**Theme-Specific Enhancements**
1. **Dark/Light Mode**
   * All elements must respect `[data-theme]` changes
   * Test contrast ratios for both modes
2. **Interactive Elements**
   * Hover effects:
```
.menu-item:hover {
  transform: translateY(-5px);
  box-shadow: 0 12px 20px var(--shadow);
}
```
   * Smooth transitions using `var(--transition)`
3. **Custom CSS Additions** (where Bootstrap falls short):
   * Gold underline accents under section headings
   * Backdrop-filter blur for menu cards in dark mode
   * Theme-aware box-shadows matching header design
**Quality Assurance**
* **Code**:
   * Semantic HTML5 (`<section>`, `<article>` for dishes)
   * ARIA labels for dietary icons (vegan/gluten-free)
   * CSS variables instead of hardcoded values
* **Visual Consistency**:
   * Match header's border-radius (recommend `8px` unless specified)
   * Gold accents must use `--primary-color`, not `#ffd700`
* **Deliverables**:
   * Fully responsive implementation (mobile → desktop)
   * Commented code explaining theme variable usage
   * Screenshots demonstrating dark/light mode support
*"The final design should feel like a natural continuation of our restaurant's sophisticated aesthetic, with every element respecting the established design system.*

Make Page functional
Note: Bootstrap, google font and font awsome icons links already added. Also dark and light theme functionality also working you just need to write code relevant to this section. Do not create theme toogle functionality again because it is already working and this is just for a reference. 
Page should be responsive, attracive, professional and modern with proper relevant elements images, icons and attractive animations,

Note: Dont create header,footer and theme toogle functionality because it is already working and this is just for a reference.
This is my theme setting:
* {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary-color: #d4af37;
            --secondary-color: #2c3e50;
            --accent-color: #e74c3c;
            --text-light: #ffffff;
            --text-dark: #2c3e50;
            --bg-light: #ffffff;
            --bg-dark: #1a1a1a;
            --shadow-light: rgba(0, 0, 0, 0.1);
            --shadow-dark: rgba(255, 255, 255, 0.1);
            --gradient-gold: linear-gradient(135deg, #d4af37, #f39c12);
            --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            --border-radius: 8px;
        }

        [data-theme="dark"] {
            --text-color: var(--text-light);
            --bg-color: var(--bg-dark);
            --header-bg: rgba(26, 26, 26, 0.95);
            --shadow: var(--shadow-dark);
            --border-color: rgba(255, 255, 255, 0.1);
        }

        [data-theme="light"] {
            --text-color: var(--text-dark);
            --bg-color: var(--bg-light);
            --header-bg: rgba(255, 255, 255, 0.95);
            --shadow: var(--shadow-light);
            --border-color: rgba(0, 0, 0, 0.1);
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: var(--bg-color);
            color: var(--text-color);
            transition: var(--transition);
            overflow-x: hidden;
        }


         const themeToggle = document.getElementById('themeToggle');
         const body = document.body;
         const themeIcon = themeToggle.querySelector('i');

         // Load saved theme or default to light
         const savedTheme = localStorage.getItem('theme') || 'light';
         body.setAttribute('data-theme', savedTheme);
         updateThemeIcon(savedTheme);

         themeToggle.addEventListener('click', () => {
            const currentTheme = body.getAttribute('data-theme');
            const newTheme = currentTheme === 'light' ? 'dark' : 'light';
            
            body.setAttribute('data-theme', newTheme);
            localStorage.setItem('theme', newTheme);
            updateThemeIcon(newTheme);
         });

         function updateThemeIcon(theme) {
            themeIcon.className = theme === 'light' ? 'fas fa-moon' : 'fas fa-sun';
         }